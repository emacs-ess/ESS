;; -*- lexical-binding: t; -*-

;; (defun face-at-point ()
;;   (get-char-property (point) 'face))

;; (defun faces-at-point ()
;;   (let ((face (get-char-property (point) 'face)))
;;     (if (listp face)
;;         face
;;       (list face))))
