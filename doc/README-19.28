

    Richard> 125 rassoc is new
    Richard>       (setq temp-ess-dialect (cdr (rassoc ess-dialect
    Richard>       ess-customize-alist)))) 
    Richard> rassoc is part of cl.el and is not automatically part of
    Richard> emacs 19.28.  As an expedient I just (require 'cl).
    Richard> emacs 19.29 seems to be happy with rassoc
    RMH> 1. ess-site.el line 50

    RMH> ;;; temporary until essd-sas is fully integrated
    RMH> (load-file "/home/a023/rmh/ESS-4.9-b16/extras.el")
    RMH> (load-file "/home/a023/rmh/ESS-4.9-b16/easymenu.el")
    RMH> (require 'cl)  ; emacs 19.28 doesn't automatically have rassoc
    RMH> ;;; temporary until essd-sas is fully integrated


    RMH> 2. Makefile
    RMH> PRELOADS =  -l extras.el -l ./ess-comp.el

    RMH> SOURCES = easymenu.el extras.el \


    RMH> 3. where extras.el is the add-to-list function from somewhere in 'cl

    RMH> (defun add-to-list (list-var element)
    RMH>   "Add to the value of LIST-VAR the element ELEMENT if it isn't there yet.
    RMH> If you want to use `add-to-list' on a variable that is not defined
    RMH> until a certain package is loaded, you should put the call to `add-to-list'
    RMH> into a hook function that will be run only after loading the package.
    RMH> `eval-after-load' provides one way to do this.  In some cases
    RMH> other hooks, such as major mode hooks, can do the job."
    RMH>   (or (member element (symbol-value list-var))
    RMH>       (set list-var (cons element (symbol-value list-var)))))


    RMH> 4. where easymenu.el is copied from 19.29
